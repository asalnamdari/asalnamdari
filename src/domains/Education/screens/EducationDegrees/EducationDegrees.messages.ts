import { defineMessages } from 'react-intl'

const scope = 'app.education.degrees'

const EducationDegreesMessages = defineMessages({
  title:{
    id:`${scope}.title`,
    defaultMessage:'مدارک حرفه‌ای بین‌المللی'
  }
})
export default EducationDegreesMessages
